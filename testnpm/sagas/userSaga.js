/*
import { call, put } from 'redux-saga/effects';
//import web3 from 'web3';
import assert from 'assert';
import { tryLogin } from '../../src/sagas/userSaga';
import login from '../../src/web3calls/login';
import { userAction } from '../../src/actions/actions';
*/
/*
describe('user saga', () => {
  describe('tryLogin()', () => {
    const generator = tryLogin();
    it('should call the web3 action for log in', () => {
      assert.deepEqual(generator.next().value, call(login, web3.eth.accounts[0]));
    });
*/

//    it('should dispatch an action for an user that is logged', () => {
//     assert.equal(generator.next().value,
//      put({ type: userAction.USER_LOGGED_IN, role: undefined }),
//      );
//    });

//      it('should be done', () => {
//      assert.equal(generator.next().done, true);
//    });
//  });
// });

